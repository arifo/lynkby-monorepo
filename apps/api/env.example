# Example Environment Variables for Local Development
# Copy this file to .env.local and fill in your values
# Note: This file is for reference only - actual secrets should be set using wrangler

# Environment
NODE_ENV=development
APP_BASE=http://localhost:8788
JWT_EXPIRES_IN=7d

# Email Configuration
EMAIL_FROM=dev@lynkby.local
SUPPORT_EMAIL=dev-support@lynkby.local
APP_NAME=Lynkby Local

# Database (for local development)
# These should be set as secrets using: wrangler secret put DATABASE_URL --env local
# 
# For local PostgreSQL (no SSL required):
# DATABASE_URL=postgresql://postgres:123456@localhost:5432/lynkby_dev
# DIRECT_URL=postgresql://postgres:123456@localhost:5432/lynkby_dev
#
# For remote database (SSL required):
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
# DIRECT_URL=postgresql://user:password@host:port/database?sslmode=require
#
# Note: The system automatically adjusts SSL mode based on NODE_ENV:
# - development/local: uses sslmode=prefer (flexible)
# - production/staging: uses sslmode=require (secure)

# JWT Secret (for local development)
# This should be set as a secret using: wrangler secret put JWT_SECRET --env local
# JWT_SECRET=your-local-jwt-secret-must-be-at-least-32-characters-long

# Revalidation Secret (for local development)
# This should be set as a secret using: wrangler secret put REVALIDATE_SECRET --env local
# REVALIDATE_SECRET=your-local-revalidation-secret

# External Services (for local development)
# These should be set as secrets using wrangler secret put
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# STRIPE_SECRET_KEY=sk_test_your_stripe_test_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret
# TIKTOK_CLIENT_KEY=your_tiktok_test_client_key
# TIKTOK_CLIENT_SECRET=your_tiktok_test_client_secret
# RESEND_API_KEY=re_your_resend_test_api_key

# Instructions:
# 1. Copy this file to .env.local
# 2. Set secrets using the setup script: ./scripts/setup-secrets.sh local
# 3. Or set secrets manually using wrangler commands
# 4. Start development server: pnpm dev
