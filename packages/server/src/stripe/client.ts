// Placeholder Stripe client wrapper for server-side usage
export function getStripe(secretKey: string) {
  // Lazy import or wrap official SDK in real implementation
  return { secretKey } as any;
}

